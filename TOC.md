📂 pure-sql-database/ (또는 설정한 리포지토리명)
├── 1. 프로젝트 개요 (Overview)
│   ├── 1.1 미션 목적 및 관계형 데이터베이스(RDB) 인프라 구축 배경
│   ├── 1.2 비즈니스 도메인(주제) 선정 배경 및 데이터 모델링 요약
│   └── 1.3 ORM(Object-Relational Mapping) 학습을 위한 관계 지향 사고방식 수립
├── 2. 실행 환경 및 도구 (Environment & Tools)
│   ├── 2.1 로컬 데이터베이스 런타임 엔지니어링 (SQLite / MySQL / PostgreSQL 중 택1)
│   ├── 2.2 GUI 클라이언트 데이터 접속 도구 명세 (DBeaver / DataGrip 등)
│   └── 2.3 백엔드 프레임워크(Spring, Django 등) 전면 배제 및 표준 SQL 범위 준수 정책
├── 3. 수행 항목 체크리스트 (Task Checklist)
│   ├── 3.1 단계별 데이터베이스 구축 마일스톤 (Step 1 ~ Step 5)
│   └── 3.2 작업 증적(Evidence) 및 3대 자산 SQL 파일 링크 매핑 테이블
├── 4. 관계형 데이터 모델(Schema) 및 무결성 제약조건 설계 (Data Modeling)
│   ├── 4.1 플랫 파일(Excel) 구조와 관계형 데이터베이스(RDB)의 아키텍처적 차이 분석
│   ├── 4.2 **4대 비즈니스 도메인 테이블 엔티티 설계**
│   │   ├── 4.2.1 핵심 부모/자식 테이블 구조 및 의미론적 데이터 타입(VARCHAR, INT, DATE) 바인딩
│   │   └── 4.2.2 기본키(PK) 설정을 통한 레코드 고유 식별성 확보
│   ├── 4.3 **참조 무결성 보장: 외래키(FK) 기반 1:N 관계(최소 2개) 설계 근거**
│   └── 4.4 데이터 도메인 하드닝을 위한 필수 제약조건(Constraints) 주입
│       ├── 4.4.1 `NOT NULL` 제약조건 적용 컬럼 명세 및 적용 사유
│       └── 4.4.2 `UNIQUE` 제약조건 적용 컬럼 명세 및 비즈니스적 예외 차단
├── 5. 샘플 데이터 프로비저닝 및 데이터 인젝션 (Data Ingestion)
│   ├── 5.1 부모-자식 테이블 간 의존성을 고려한 `INSERT INTO` 쿼리 실행 순서 프로세스
│   ├── 5.2 데이터 가용성 확보: 각 테이블별 10행(Row) 이상의 의미 있는 데이터셋 구성
│   └── 5.3 참조 무결성 제약 조건 하부 데이터 정합성 정밀 검증
├── 6. 실무 요구사항 해결을 위한 핵심 SQL 쿼리 15개 벤치마킹 (Query Engineering)
│   ├── 6.1 **필터링 및 정렬 기본 조회 스펙 (4개 이상)**
│   │   └── `WHERE` 조건절 연산, `ORDER BY` 다중 정렬, `LIMIT` 페이징 격리 쿼리
│   ├── 6.2 **테이블 간 관계 복원 및 수평 결합 조인 쿼리 (4개 이상)**
│   │   ├── 6.2.1 데이터 무결성 기준 `INNER JOIN` 연산 (2개 이상)
│   │   └── 6.2.2 빈 데이터 추적용 교차 결합 `LEFT OUTER JOIN` 연산 (1개 이상)
│   ├── 6.3 **데이터 통계 및 다차원 분석 집계 쿼리 (3개 이상)**
│   │   └── 집계 함수(`COUNT`, `SUM`, `AVG` 중 2개 이상)와 `GROUP BY`절 연동 데이터 그룹화
│   ├── 6.4 **중첩 질의 및 동적 조건 서브쿼리(Subquery) 구현 (1개 이상)**
│   ├── 6.5 **데이터 변동 제어 및 삭제 트랜잭션 (2개 이상)**
│   │   └── 조건부 데이터 갱신(`UPDATE`) 및 데이터 행 영구 격리(`DELETE`)
│   └── 6.6 **인덱스(CREATE INDEX) 설계 및 특정 컬럼 적용 사유 기술 (1개 이상)**
├── 7. [보너스 영역] 쿼리 최적화 및 정합성 파괴 테스트 (Advanced Performance)
│   ├── 7.1 단일 비즈니스 요구사항에 대한 `JOIN` 연산 vs `Subquery` 구조적 성능 트레이드오프 분석
│   ├── 7.2 의도적 데이터 정합성 파괴 시나리오: 고의적 FK 참조 오류 유발 및 RDBMS 차단 메시지 역추적
│   └── 7.3 핵심 비즈니스 도메인 KPI 지표 산출용 마스터 리포트 쿼리 3선 정의
├── 8. 트러블슈팅 리포트 (Troubleshooting)
│   ├── 8.1 [Issue #1] 자식 테이블 데이터 인젝션 중 부모 테이블 미존재 키 참조로 인한 FK 위반 오류
│   │   └── (상황 및 현상 → 원인 분석 및 가설 설정 → 실행 순서 교정 → 최종 해결 조치 및 멱등성 확보)
│   └── 9.2 [Issue #2] 집계 함수(`GROUP BY`) 연산 시 비집계 컬럼 혼용으로 인한 표준 SQL 구문 에러
│       └── (상황 및 현상 → 원인 분석 및 가설 설정 → 안티 패턴 교정 → 최종 해결 조치 및 무결성 통과)
└── 9. 엔지니어링 인사이트 및 결론 (Insights & Conclusion)
    ├── 9.1 논리적 데이터 모델(ERD) 관계 설계가 애플리케이션 아키텍처에 미치는 영향
    └── 9.2 고급 데이터베이스 오브젝트(View, Index, Procedure)의 장기 운영 관리적 고찰
